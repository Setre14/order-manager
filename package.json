{
  "name": "order-manager",
  "version": "0.1.1",
  "scripts": {
    "postinstall": "subpkg install",
    "publish": "subpkg publish --access public"
  },
  "private": false,
  "subPackages": [
    "shared",
    "server",
    "client"
  ],
  "devDependencies": {
    "subpackage": "^1.1.0"
  }
}
